# Pure SWE-ReBench resolved native-tool SFT (225)

Contains 225 verifier-resolved SWE-ReBench-v2 trajectories and zero SWE-Bench Verified rows.
For 63 records, one unexecuted harness-rejected turn and its error prompt were removed. Every remaining assistant turn has exactly one native bash call; tool results and harness runtime feedback retain their original mini-swe-agent roles.

Use `train.jsonl` for SFT and `records.jsonl` for provenance.
